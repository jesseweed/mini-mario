head
  meta(charset='utf-8')
  meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
  meta(name='viewport', content='width=device-width,initial-scale=1')

  title [mm] miniMARIO

  meta(http-equiv='Content-type', content='text/html;charset=UTF-8')

  link(rel='stylesheet', href='#{site.dir.css}styles.css')

  //- Allows you to pass an array of page specific stylesheets to load
  if locals.css
    each stylesheet in css
      link(rel='stylesheet', href='#{site.dir.css}#{stylesheet}')

  script(src='#{site.dir.lib}headjs/dist/1.0.0/head.min.js')

  script.
    head.load("#{site.dir.js}pong.js", function() {
      Pong.init({
        css: '#{site.dir.css}',
        img: '#{site.dir.img}',
        js: '#{site.dir.js}',
        lib: '#{site.dir.lib}'
      });
    });
